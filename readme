kms工具可放置于任何路径执行
cd 进入到脚本所在路径后通过./add-kms.sh 或者./make-iso.sh执行

add-kms.sh
用于给iso增加kms相关配置
执行后会自动检索iso路径，用数字键选择需要增加kms功能的路径。

make-iso.sh
用于iso的打包
执行后会自动检索iso路径，打包后的会存放在脚本所在路径下的iso文件夹中。
请自行修改新生成的iso文件名。
